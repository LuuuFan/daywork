<%= form_with(model: cheer, local: true) do |form| %>
  <% if cheer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cheer.errors.count, "error") %> prohibited this cheer from being saved:</h2>

      <ul>
      <% cheer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :cheer_user_id %>
  </div>

  <div class="field">
    <%= form.label :goal_id %>
    <%= form.text_field :goal_id, id: :cheer_goal_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
